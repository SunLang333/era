# SLG/SCENARIO/DIPRO_COLOR.ERB — 自动生成文档

源文件: `ERB/SLG/SCENARIO/DIPRO_COLOR.ERB`

类型: .ERB

自动摘要: functions: CHARA_DIPRO_COLOR

前 200 行源码片段:

```text
﻿
;-------------------------------------------------------------------------------
;旧作を除く全キャラの固有国カラー
;-------------------------------------------------------------------------------
@CHARA_DIPRO_COLOR(ARGS:0)
LOCAL:0 = FINDCHARA(CSTR:99, ARGS:0)

IF LOCAL:0 == FINDCHARA(CSTR:99, "霊夢")
	RETURN 0xa3a3a2
;薄墨色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "魔理沙")
	RETURN 0xc1ab05
;アンティークゴールド
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "ルーミア")
	RETURN 0Xfcc800
;クロムイエロー
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "大妖精")
	RETURN 0Xa3d6cc
;アイスグリーン
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "チルノ")
	RETURN 0xa1d8e2
;サマーシャワー
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "美鈴")
	RETURN 0Xed6d46
;チャイナレッド
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "小悪魔")
	RETURN 0X6c3524
;チョコ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "パチュリー")
	RETURN 0Xa688bd
;ラベンダーモーヴ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "咲夜")
	RETURN 0X736d71
;スチールグレイ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "レミリア")
	RETURN 0xea5532
;スカーレット
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "フランドール")
	RETURN 0Xe9bc00
;トパーズ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "レティ")
	RETURN 0Xe6eae6
;フロスティホワイト
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "橙")
	RETURN 0Xee7800
;橙
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "アリス")
	RETURN 0Xeddc44
;シトラス
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "リリーホワイト")
	RETURN 0Xefefef
;シルバーホワイト
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "リリーブラック")
	RETURN 0Xafafb0
;シルバーグレイ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "リリカ")
	RETURN 0Xf3a68c
;サーモンピンク
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "メルラン")
	RETURN 0Xbcbace
;ラベンダーグレイ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "ルナサ")
	RETURN 0X9d8e87
;グリーンフォグ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "妖夢")
	RETURN 0X6b7b6e
;青鈍色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "幽々子")
	RETURN 0xfdeff2
;薄桜色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "藍")
	RETURN 0X165e83
;藍
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "紫")
	RETURN 0x65318e
;本紫
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "萃香")
	RETURN 0Xd8a373
;キャラ色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "リグル")
	RETURN 0X9fc24d
;リーフグリーン
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "ミスティア")
	RETURN 0Xaa4f37
;雀茶
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "慧音")
	RETURN 0x974E00
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "てゐ")
	RETURN 0Xf7fcfe
;卯の花色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "鈴仙")
	RETURN 0X513743
;偽紫
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "永琳")
	RETURN 0x915c8b
;二藍
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "輝夜")
	RETURN 1
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "妹紅")
	RETURN 0Xec6d71
;真朱
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "文")
	RETURN 0Xfff1cf
;鳥の子色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "メディスン")
	RETURN 0Xe8ecef
;白花色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "幽香")
	RETURN 0Xfcc801
;向日葵色（ルーミアと同じ）
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "小町")
	RETURN 0XE95464
;韓紅
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "四季映姫")
	RETURN 0x19448e
;瑠璃紺色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "静葉")
	RETURN 0Xdb8449
;赤朽葉色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "穣子")
	RETURN 0Xe49e61
;小麦色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "雛")
	RETURN 0xada250
;青朽葉色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "にとり")
	RETURN 0Xbce2e8
;水色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "椛")
	RETURN 0xcd5e3c
;樺（椛）色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "早苗")
	RETURN 0Xc3d825
;若草色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "神奈子")
	RETURN 0xbbbcde
;藤色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "諏訪子")
	RETURN 0X005243
;鉄色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "サニーミルク")
	RETURN 0Xffedab
;サンシャインイエロー
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "ルナチャイルド")
	RETURN 0xe5a323
;パンプキン
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "スターサファイア")
	RETURN 0X26499d
;オリエンタルブルー
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "阿求")
	RETURN 0Xdf7163
;浅緋
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "蓮子")
	RETURN 0X4a488e
;紺藍
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "マエリベリー")
	RETURN 0x884898
;紫
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "衣玖")
	RETURN 0X8491c3
;紅掛空色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "天子")
	RETURN 0x2ca9e1
;空色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "豊姫")
	RETURN 0Xe7e7eb
;紫水晶
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "依姫")
	RETURN 0Xeaf4fc
;月白色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "レイセン")
	RETURN 0Xeae5e3
;素色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "キスメ")
	RETURN 0X69821b
;苔色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "ヤマメ")
	RETURN 0Xfabf14
;ウコン色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "パルスィ")
	RETURN 0X00552e
;深緑
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "勇儀")
	RETURN 0Xf8b500
;山吹色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "さとり")
	RETURN 0xea618e
;ローズレッド
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "燐")
	RETURN 0Xc9171e
;深緋
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "空")
	RETURN 0Xd7cf3a
;鶸色
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "こいし")
	RETURN ;898880
;ストーングレイ
ELSEIF LOCAL:0 == FINDCHARA(CSTR:99, "朱鷺子")
	RETURN 0Xf4b3c2
;朱鷺色
```
