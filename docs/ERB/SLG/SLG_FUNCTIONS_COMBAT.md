# SLG/SLG_FUNCTIONS_COMBAT.ERB — 自动生成文档

源文件: `ERB/SLG/SLG_FUNCTIONS_COMBAT.ERB`

类型: .ERB

自动摘要: functions: SET_BATTLE_MIRROR_VAL, BATTLE_COMMANDER_WITHDRAWAL, BATTLE_KNOCKOUT, ATTACK_LEVEL, DEFENSE_LEVEL, INTELLIGENCE_LEVEL, MAGIC_LEVEL, SUPPORT_LEVEL, SLG_COMBAT_BEAT_TREATY_TARGET, PRINT_UNIT_INFO_COMBAT, PRINT_COMBAT_INFO, PRINT_CITY_INFO_COMBAT, SUM_STARS_COMBAT, GET_COMMANDER_NAME_COMBAT; UI/print

前 200 行源码片段:

```text
﻿;-------------------------------------------
;戦闘計算で使用するミラー変数の用意
;-------------------------------------------
@SET_BATTLE_MIRROR_VAL(SIDE, CHARA:0 = -1, CHARA:1 = -1, CHARA:2 = -1)
#DIM SIDE
#DIM CHARA, 3
SIF !GROUPMATCH(SIDE, 0, 1)
	THROW SIDEには0か1を代入してください

FOR LOCAL, 0, VARSIZE("BATTLE_COMMANDER", 1)
	BATTLE_COMMANDER:SIDE:LOCAL = -1
	BATTLE_武闘:SIDE:LOCAL = 0
	BATTLE_防衛:SIDE:LOCAL = 0
	BATTLE_知略:SIDE:LOCAL = 0
	BATTLE_政治:SIDE:LOCAL = 0
	BATTLE_歌唱:SIDE:LOCAL = 0
	BATTLE_料理:SIDE:LOCAL = 0
	BATTLE_妖術:SIDE:LOCAL = 0
	BATTLE_武闘パワー:SIDE:LOCAL = 0
	BATTLE_防衛パワー:SIDE:LOCAL = 0
	BATTLE_知略パワー:SIDE:LOCAL = 0
	BATTLE_妖術パワー:SIDE:LOCAL = 0
	BATTLE_歌唱パワー:SIDE:LOCAL = 0
	BATTLE_料理パワー:SIDE:LOCAL = 0
	BATTLE_SKILL_RATE:SIDE:LOCAL = 0

NEXT
BATTLE_COMMANDER_NUM:SIDE = 0

FOR LOCAL:0, 0, VARSIZE("CHARA")
	SIF CHARA:LOCAL < 0 || CHARA:LOCAL > CHARANUM
		BREAK
	BATTLE_COMMANDER_NUM:SIDE ++
	BATTLE_COMMANDER:SIDE:LOCAL = CHARA:LOCAL
	BATTLE_武闘:SIDE:LOCAL = ABL:(CHARA:LOCAL):武闘
	BATTLE_防衛:SIDE:LOCAL = ABL:(CHARA:LOCAL):防衛
	BATTLE_知略:SIDE:LOCAL = ABL:(CHARA:LOCAL):知略
	BATTLE_政治:SIDE:LOCAL = ABL:(CHARA:LOCAL):政治
	BATTLE_歌唱:SIDE:LOCAL = ABL:(CHARA:LOCAL):歌唱
	BATTLE_料理:SIDE:LOCAL = ABL:(CHARA:LOCAL):料理
	BATTLE_妖術:SIDE:LOCAL = ABL:(CHARA:LOCAL):妖術
	BATTLE_武闘パワー:SIDE:LOCAL = ABL_POWER(BATTLE_武闘:SIDE:LOCAL, CHARA:LOCAL)
	BATTLE_防衛パワー:SIDE:LOCAL = ABL_POWER(BATTLE_防衛:SIDE:LOCAL, CHARA:LOCAL)
	BATTLE_知略パワー:SIDE:LOCAL = ABL_POWER(BATTLE_知略:SIDE:LOCAL, CHARA:LOCAL)
	SIF BATTLE_妖術:SIDE:LOCAL
		BATTLE_妖術パワー:SIDE:LOCAL = ABL_POWER(BATTLE_妖術:SIDE:LOCAL, CHARA:LOCAL)
	BATTLE_歌唱パワー:SIDE:LOCAL = ABL_POWER(BATTLE_歌唱:SIDE:LOCAL, CHARA:LOCAL)
	BATTLE_料理パワー:SIDE:LOCAL = ABL_POWER(BATTLE_料理:SIDE:LOCAL, CHARA:LOCAL)
	BATTLE_SKILL_RATE:SIDE:LOCAL = 100
NEXT

;-------------------------------------------------
;BATTLE:SIDE側のCOM_NO番が戦闘中に離脱した
;実際の部隊データからは外れないので注意(別途FORCE_FREE打て)
;-------------------------------------------------
@BATTLE_COMMANDER_WITHDRAWAL(SIDE, COM_IDX)
#DIM SIDE
#DIM COM_IDX

;最後尾を指定した場合はゼロフィル(シフトすると配列外参照で死にかねない)
IF COM_IDX == BATTLE_COMMANDER_NUM:SIDE - 1
	BATTLE_武闘:SIDE:COM_IDX = 0
	BATTLE_防衛:SIDE:COM_IDX = 0
	BATTLE_知略:SIDE:COM_IDX = 0
	BATTLE_政治:SIDE:COM_IDX = 0
	BATTLE_妖術:SIDE:COM_IDX = 0
	BATTLE_料理:SIDE:COM_IDX = 0
	BATTLE_歌唱:SIDE:COM_IDX = 0
	BATTLE_武闘パワー:SIDE:COM_IDX = 0
	BATTLE_防衛パワー:SIDE:COM_IDX = 0
	BATTLE_知略パワー:SIDE:COM_IDX = 0
	BATTLE_妖術パワー:SIDE:COM_IDX = 0
	BATTLE_歌唱パワー:SIDE:COM_IDX = 0
	BATTLE_料理パワー:SIDE:COM_IDX = 0
	BATTLE_SKILL_RATE:SIDE:COM_IDX = 0
	BATTLE_COMMANDER:SIDE:COM_IDX = -1
;最後尾以外を指定した場合はシフトして対応
;どうせCOMMANDER_NUM上限でループするやろしシフト元の値は放置でええやろ
ELSE
	FOR LOCAL, COM_IDX, BATTLE_COMMANDER_NUM:SIDE - 1
		BATTLE_武闘:SIDE:LOCAL = BATTLE_武闘:SIDE:(LOCAL + 1)
		BATTLE_防衛:SIDE:LOCAL = BATTLE_防衛:SIDE:(LOCAL + 1)
		BATTLE_知略:SIDE:LOCAL = BATTLE_知略:SIDE:(LOCAL + 1)
		BATTLE_政治:SIDE:LOCAL = BATTLE_政治:SIDE:(LOCAL + 1)
		BATTLE_妖術:SIDE:LOCAL = BATTLE_妖術:SIDE:(LOCAL + 1)
		BATTLE_料理:SIDE:LOCAL = BATTLE_料理:SIDE:(LOCAL + 1)
		BATTLE_歌唱:SIDE:LOCAL = BATTLE_歌唱:SIDE:(LOCAL + 1)
		BATTLE_武闘パワー:SIDE:LOCAL = BATTLE_武闘パワー:SIDE:(LOCAL + 1)
		BATTLE_防衛パワー:SIDE:LOCAL = BATTLE_防衛パワー:SIDE:(LOCAL + 1)
		BATTLE_知略パワー:SIDE:LOCAL = BATTLE_知略パワー:SIDE:(LOCAL + 1)
		BATTLE_妖術パワー:SIDE:LOCAL = BATTLE_妖術パワー:SIDE:(LOCAL + 1)
		BATTLE_歌唱パワー:SIDE:LOCAL = BATTLE_歌唱パワー:SIDE:(LOCAL + 1)
		BATTLE_料理パワー:SIDE:LOCAL = BATTLE_料理パワー:SIDE:(LOCAL + 1)
		BATTLE_SKILL_RATE:SIDE:LOCAL = BATTLE_SKILL_RATE:SIDE:(LOCAL + 1)
		BATTLE_COMMANDER:SIDE:LOCAL = BATTLE_COMMANDER:SIDE:(LOCAL + 1)
	NEXT
ENDIF

BATTLE_COMMANDER_NUM:SIDE -= 1

;---------------------------------
;暗殺の効果用
;---------------------------------
@BATTLE_KNOCKOUT(対象側, 対象番号)
#DIM 対象側
#DIM 対象番号
BATTLE_武闘:対象側:対象番号 = 0
BATTLE_防衛:対象側:対象番号 = 0
BATTLE_知略:対象側:対象番号 = 0
BATTLE_政治:対象側:対象番号 = 0
BATTLE_妖術:対象側:対象番号 = 0
BATTLE_料理:対象側:対象番号 = 0
BATTLE_歌唱:対象側:対象番号 = 0
BATTLE_武闘パワー:対象側:対象番号 = 0
BATTLE_防衛パワー:対象側:対象番号 = 0
BATTLE_知略パワー:対象側:対象番号 = 0
BATTLE_妖術パワー:対象側:対象番号 = 0
BATTLE_歌唱パワー:対象側:対象番号 = 0
BATTLE_料理パワー:対象側:対象番号 = 0
BATTLE_SKILL_RATE:対象側:対象番号 = 0
BATTLE_SKILL_SEALED:対象側:対象番号 = 1



;-------------------------------------------------
;BATTLE:SIDE側の攻撃能力を決定する。
;CITY_NOが1以上で、対応するCITY_TYPEが0なら、防衛部隊とみなす。
;-------------------------------------------------
@ATTACK_LEVEL(SIDE, CITY_NO = 0)
#FUNCTION
#DIM SIDE
#DIM CITY_NO
#DIM CURRENT_CHARA
#DIM ATTACK
ATTACK = 0

;だれもいない場合、能力値0の武将がひとりいるものとみなす。
IF BATTLE_COMMANDER_NUM:SIDE == 0
	ATTACK = ABL_POWER(0, -1)
	GOTO BASE_CALCED
ENDIF

;誰かいる場合、対応能力と妖術から算出する。
FOR LOCAL:0, 0, BATTLE_COMMANDER_NUM:SIDE
	CURRENT_CHARA = BATTLE_COMMANDER:SIDE:(LOCAL:0)
	SIF CURRENT_CHARA < 0
		CONTINUE
	ATTACK += BATTLE_武闘パワー:SIDE:LOCAL
	ATTACK += BATTLE_妖術パワー:SIDE:LOCAL / 3
NEXT

$BASE_CALCED

ATTACK = ATTACK * SUPPORT_LEVEL(SIDE, CITY_NO) / 1000

;防衛部隊用の計算式
IF CITY_NO && CITY_TYPE:CITY_NO == 0
	RETURNF ATTACK / 2 * MAX(CITY_GUARD_RATE(CITY_NO) - 100, 100) / 100
ENDIF

RETURNF MAX(ATTACK / 3, 1)

;-------------------------------------------------
;BATTLE:SIDE側の防御能力を決定する。
;CITY_NOが1以上で、対応するCITY_TYPEが0なら、防衛部隊とみなす。
;-------------------------------------------------
@DEFENSE_LEVEL(SIDE, CITY_NO = 0)
#FUNCTION
#DIM SIDE
#DIM CITY_NO
#DIM CURRENT_CHARA
#DIM DEFENSE
DEFENSE = 0

;だれもいない場合、能力値0の武将がひとりいるものとみなす。
IF BATTLE_COMMANDER_NUM:SIDE == 0
	DEFENSE = ABL_POWER(0, -1)
	GOTO BASE_CALCED
ENDIF

;誰かいる場合、対応能力と妖術から算出する。
FOR LOCAL:0, 0, BATTLE_COMMANDER_NUM:SIDE
	CURRENT_CHARA = BATTLE_COMMANDER:SIDE:(LOCAL:0)
	SIF CURRENT_CHARA < 0
		CONTINUE
	DEFENSE += BATTLE_防衛パワー:SIDE:LOCAL
	DEFENSE += BATTLE_妖術パワー:SIDE:LOCAL / 3
NEXT

$BASE_CALCED

DEFENSE = DEFENSE * SUPPORT_LEVEL(SIDE, CITY_NO) / 1000

;防衛部隊用の計算式
;無所属都市は防衛倍率を勘案しない
IF CITY_NO && CITY_TYPE:CITY_NO == 0
	IF IS_COUNTRY(CITY_OWNER:CITY_NO)
		DEFENSE = DEFENSE * CITY_GUARD_RATE(CITY_NO) / 100
	ENDIF
	RETURNF DEFENSE
```
