# TRAIN/COMF/COMF505_料理を作る.ERB — 自动生成文档

源文件: `ERB/TRAIN/COMF/COMF505_料理を作る.ERB`

类型: .ERB

自动摘要: functions: COM_NAME505, COM_ABLE505, COM505, COM_TEXT_BEFORE505, COM_TEXT_LAST505, COM_AVAILABLE_WHEN505; assigns RESULTS; UI/print

前 200 行源码片段:

```text
﻿;料理を作る

;-------------------------------------------------
;コマンド名称
;-------------------------------------------------
@COM_NAME505
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):子の成長度 <= 2
	RESULTS = 料理を作る
ELSE
	RESULTS = 料理を作る
ENDIF

;-------------------------------------------------
;選択可否判定
;-------------------------------------------------
@COM_ABLE505
;共通部分
CALL COM_ABLE_COMMON(505)
SIF RESULT == 0
	RETURN 0
;主人公がプレイヤーのとき限定
SIF MPLY:0 != MASTER
	RETURN 0
RETURN 1

;-------------------------------------------------
;メイン処理
;-------------------------------------------------
@COM505
CALL COM_SLG_TRAIN_MAIN_PROCESS(GETNUM(ABL, "料理"), 1)

RETURN 1

;-------------------------------------------------
;地の文(前文)
;-------------------------------------------------
@COM_TEXT_BEFORE505
;料理を作る
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):子の成長度 <= 2
	PRINTFORMW %ANAME(MPLY:0)%は%ANAME(MTAR:0)%と一緒に料理を作ることにした

;料理を作る
ELSE
	PRINTFORMW %ANAME(MPLY:0)%は%ANAME(MTAR:0)%と一緒に料理を作ることにした
ENDIF

;--------------------------------------------------------
;地の文(パラメータ・刻印変動後)
;--------------------------------------------------------
@COM_TEXT_LAST505

;-------------------------------------------------

;コマンド区分
;-------------------------------------------------

@COM_AVAILABLE_WHEN505
RETURN コマンド_育児
```
