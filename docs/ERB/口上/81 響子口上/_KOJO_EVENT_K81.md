# 口上/81 響子口上/_KOJO_EVENT_K81.ERB — 自动生成文档

源文件: `ERB/口上/81 響子口上/_KOJO_EVENT_K81.ERB`

类型: .ERB

自动摘要: functions: KOJO_EVENT_K81; UI/print

前 200 行源码片段:

```text
﻿;-------------------------------------------------
;イベント口上
;-------------------------------------------------

;=================================================
;●各種イベント
;※ARGにイベント番号が入る。詳しくは資料フォルダの「era恋姫 イベント表」を参照
;※RETURNの値を0→1に変えると、デフォルトのメッセージが表示されなくなる
;=================================================
@KOJO_EVENT_K81(ARG)

;-------------------------------------------------
;ファーストキス実行
;-------------------------------------------------
IF ARG == 1
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		;PRINTFORMW 
	;恋人
	ELSEIF TALENT:恋人
		;PRINTFORMW 
	;それ以外
	ELSE
		;PRINTFORMW 
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;告白成功
;-------------------------------------------------
IF ARG == 2
	SETCOLOR 0x088A08
	PRINTFORML 
	PRINTFORMW 「…自分と、恋人になって欲しい………っ、って、え！？　わ、わたしとっ！？」
	PRINTFORML 
	PRINTFORMW %ANAME(MASTER)%の告白をいつものように条件反射でオウム返しした後、彼女はその言葉の意味を理解した
	PRINTFORML 
	PRINTFORMW 「あ、あのっ、その…。ほ、本当にわたしと…、こ、恋人になってくれるの？」
	PRINTFORML 
	PRINTFORMW ― 自分はそのつもりだか…。%ANAME(TARGET)%にその気は無いだろうか… ―
	PRINTFORML 
	PRINTFORMW 「い、いえっ！　あります、ありますっ！　こ、こっちこそ！　こ、恋人にしてくださいっ！！」
	PRINTFORML 
	PRINTFORML そう言って%ANAME(TARGET)%は%ANAME(MASTER)%に駆け寄ろうとしたが、慌てすぎてつまづいてしまった
	PRINTFORMW %ANAME(MASTER)%は彼女が倒れないように優しく抱きとめる。自然と二人は抱き合うような姿勢になっていた
	PRINTFORML 
	PRINTFORMW 「あ、あはは…。慌てすぎちゃった！　…でも嬉しい」
	PRINTFORML 
	PRINTFORMW 「こういう風に恋人同士で抱き合うのが、わたしの夢だったの…♪」
	PRINTFORML 
	PRINTFORMW 互いの温もりを確かめ合うように、二人はしばしの間抱きしめ合っていた……
	PRINTFORML 
	RESETCOLOR
	RETURN 1
ENDIF

;-------------------------------------------------
;告白失敗
;-------------------------------------------------
IF ARG == 3
	PRINTL 
	PRINTFORMW 「ご、ごめんなさい……」
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し成功(酒酔いの影響・合意は得られない)
;-------------------------------------------------
IF ARG == 4
	PRINTL 
	PRINTFORMW 「はえ～？　にゃに～？？」
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し成功(合意を取得)
;-------------------------------------------------
IF ARG == 5
	PRINTL 
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		PRINTFORML 「きゃっ！　……わ、わたしと…するの…？」
		PRINTFORML %ANAME(TARGET)%は戸惑いつつも、%ANAME(MASTER)%に身を委ねている
		PRINTFORMW 「ちょ、ちょっと怖いけど…、%ANAME(MASTER)%にだったら、いいよ…♥」
	;それ以外
	ELSE
		PRINTFORMW 「きゃっ！　え、え！？　何？」
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し失敗
;-------------------------------------------------
IF ARG == 6
	PRINTFORMW 「誰か男の人呼んで―――ッッ！！」
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し成功(既に合意あり)
;-------------------------------------------------
IF ARG == 7
	PRINTL 
	SELECTCASE RAND:3
		CASE 0
			PRINTFORML 「きゃっ！　……わ、わたしと…するの…？」
			PRINTFORML %ANAME(TARGET)%は戸惑いつつも、%ANAME(MASTER)%に身を委ねている
			PRINTFORMW 「うん、いいよ…♥　でも優しくしてね？」
		CASE 1
			PRINTFORML 帰り際、名残惜しげにしていた%ANAME(TARGET)%を強く抱き寄せる
			PRINTFORML 「あっ…。…ふふ、今日は朝まで一緒なんだね♥」
			PRINTFORMW %ANAME(TARGET)%は力を抜いて、%ANAME(MASTER)%のことを受け入れた…
		CASE 2
			PRINTFORML 「きゃっ！　……き、今日もするんだね。…えへへ♪」
			PRINTFORML %ANAME(TARGET)%はなすがまま、笑顔で%ANAME(MASTER)%を受け入れている
			PRINTFORMW 「優しくしてね？　%ANAME(MASTER)%…♥」
	ENDSELECT
	RETURN 0
ENDIF

;-------------------------------------------------
;:体力限界(通常)
;-------------------------------------------------
IF ARG == 11
	PRINTFORMW 「もっ、もうだめぇっ……」
	RETURN 0
ENDIF

;-------------------------------------------------
;気力限界(通常)
;-------------------------------------------------
IF ARG == 12
	PRINTFORMW 「も、もう無理ぃ……」
	RETURN 0
ENDIF

;-------------------------------------------------
;怒りの限界で追い返される
;-------------------------------------------------
IF ARG == 13
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;哀しみの限界で追い返される
;-------------------------------------------------
IF ARG == 14
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;怒りの限界で勝手に帰る
;-------------------------------------------------
IF ARG == 15
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;哀しみの限界で勝手に帰る
;-------------------------------------------------
IF ARG == 16
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;体力限界(捕虜調教)
;-------------------------------------------------
IF ARG == 17
	PRINTFORMW 「もっ、もうゆるしてぇ……」
	RETURN 0
ENDIF

 ;-------------------------------------------------
;気力限界(捕虜調教)
;-------------------------------------------------
IF ARG == 18
	PRINTFORMW 「もういやぁ…助けて…」
	RETURN 0
ENDIF

 ;-------------------------------------------------
;捕虜調教で叩き起こす
;-------------------------------------------------
IF ARG == 19
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;デート中に向こうからキス
;-------------------------------------------------
IF ARG == 20
	;PRINTFORMW 
	RETURN 0
```
