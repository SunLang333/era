# 口上/111 幻月口上/_KOJO_COMMON_K111.ERB — 自动生成文档

源文件: `ERB/口上/111 幻月口上/_KOJO_COMMON_K111.ERB`

类型: .ERB

自动摘要: functions: KOJO_EXIST_K111, KOJO_TRAIN_START_K111, KOJO_TRAIN_END_K111, KOJO_COM_BEFORE_TARGET_K111, KOJO_COM_BEFORE_PLAYER_K111, KOJO_COM_TARGET_K111, KOJO_COM_PLAYER_K111, KOJO_COM_BEFORE_K111, KOJO_COM_AFTER_K111, KOJO_SINGLE_ENDING_K111, KOJO_DEAD_ENDING_K111; UI/print

前 200 行源码片段:

```text
﻿;-------------------------------------------------
;共通部分
;-------------------------------------------------

;=================================================
;●●口上開発用テンプレ●●
;PRINTFORMW クリック待ち
;PRINTFORML クリック待たない
;%ANAME(幻月)%
;%ANAME(幻月_対象)%
;SETCOLOR COLOR("XXX") 色指定
; 色指定解除

;=================================================
;●口上の存在判定
;=================================================
@KOJO_EXIST_K111

;=================================================
;●開始時
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_TRAIN_START_K111

;=================================================
;●終了時
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_TRAIN_END_K111

;=================================================
;●コマンド実行前(このキャラがターゲット側のとき)
;※地の文が表示される前に実行される。戻り値に1を設定する(RETURN 1)と地の文がカットされる
;  必要に応じてKOJO_COM_TARGETの代わりに使う
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_COM_BEFORE_TARGET_K111
RETURN 0

;=================================================
;●コマンド実行前(このキャラがプレイヤー側のとき)
;※地の文が表示される前に実行される。戻り値に1を設定する(RETURN 1)と地の文がカットされる
;  必要に応じてKOJO_COM_PLAYERの代わりに使う
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_COM_BEFORE_PLAYER_K111
RETURN 0

;=================================================
;●コマンド実行時(このキャラがプレイヤー側のとき)
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_COM_TARGET_K111

;=================================================
;●コマンド実行時(このキャラがプレイヤー側のとき)
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_COM_PLAYER_K111

;=================================================
;●コマンド実行前(ターゲット・プレイヤー問わず)
;※地の文が表示される前に実行される。戻り値に1を設定する(RETURN 1)と地の文がカットされる
;  必要に応じてKOJO_COM_TARGETの代わりに使う
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_COM_BEFORE_K111
RETURN 0

;=================================================
;●コマンド実行後(パラメータの変動処理が終わってから呼び出される)
;※状況にかかわらず常に実行される。取り扱い注意※
;=================================================
@KOJO_COM_AFTER_K111
#DIM 幻月
#DIM 幻月_対象
#DIM CONST 初めてのC絶頂 = 237
#DIM CONST 初めてのV絶頂 = 238
#DIM CONST 初めてのA絶頂 = 239
#DIM CONST 初めてのB絶頂 = 240
#DIM CONST 初めてのM絶頂 = 241

;名前の定義
幻月 = NAME_TO_CHARA("幻月")
幻月_対象 = MASTER
;[虚ろ]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:虚ろ || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF
;-------------------------------------------------
;初絶頂は使い回せそうなのでここに作成
;-------------------------------------------------
;-------------------------------------------------
;初めてＣ絶頂
;-------------------------------------------------
IF NOWEX:Ｖ絶頂 > 0
	;初めてのC絶頂
	IF CFLAG:TARGET:初めてのC絶頂 == 0
		CFLAG:TARGET:初めてのC絶頂 += 1
		PRINTFORMW 「んん...!!」
		PRINTFORMW %ANAME(幻月)%は体をビクッとさせ達した
		PRINTFORMW 「他人にイカされると、なんか恥ずかしいわね...」
	;次回以降
	ELSE
		CFLAG:TARGET:初めてのC絶頂 += 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;初めてＶ絶頂
;-------------------------------------------------
IF NOWEX:Ｖ絶頂 > 1
	;初めてのV絶頂
	IF CFLAG:TARGET:初めてのV絶頂 == 0
		CFLAG:TARGET:初めてのV絶頂 += 1
		PRINTFORMW 「イッｸｩ!」
		PRINTFORMW 「っ————!!」
		PRINTFORMW %ANAME(幻月)%は%ANAME(幻月_対象)%の手によってイカされた...
		PRINTFORMW 「あなた結構、上手いわ...」
		PRINTFORMW %ANAME(幻月)%は顔を上気させながらそう呟いた...
	;次回以降
	ELSE
		CFLAG:TARGET:初めてのV絶頂 += 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;初めてＡ絶頂
;-------------------------------------------------
IF NOWEX:Ａ絶頂 > 1
	;初めてのA絶頂
	IF CFLAG:TARGET:初めてのA絶頂 == 0
		CFLAG:TARGET:初めてのA絶頂 += 1
		PRINTFORMW 「クルｯ!!きちゃうのおぉ！」
		PRINTFORMW 「————ｯ!!」
		PRINTFORMW 「ま、まさかお尻でイかされるなんて...」
		PRINTFORMW %ANAME(幻月)%は肩で息をしながら続けた
		PRINTFORMW 「私の体、もとに戻るかなぁ？」
		PRINTFORMW 「もう、手遅れかな...」
		PRINTFORMW そう呟く%ANAME(幻月)%であったが、その穴はヒクつき
		PRINTFORML 次の挿入を待ちわびている...
		PRINTFORMW
	;次回以降
	ELSE
		CFLAG:TARGET:初めてのA絶頂 += 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;初めてＢ絶頂
;-------------------------------------------------
IF NOWEX:Ｂ絶頂 > 1
	;初めてのB絶頂
	IF CFLAG:TARGET:初めてのB絶頂 == 0
		CFLAG:TARGET:初めてのB絶頂 += 1
		;PRINTFORMW 
	;次回以降
	ELSE
		CFLAG:TARGET:初めてのB絶頂 += 1
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;初めてＭ絶頂
;-------------------------------------------------
IF NOWEX:Ｍ絶頂 > 1
	;初めてのM絶頂
	IF CFLAG:TARGET:初めてのM絶頂 == 0
		CFLAG:TARGET:初めてのM絶頂 += 1
		;PRINTFORMW 
	;次回以降
	ELSE
		CFLAG:TARGET:初めてのM絶頂 += 1
		;PRINTFORMW 
	ENDIF
ENDIF

;=================================================
;●単体エンディング関係
;=================================================
;
;
;
;
;
;
;
;
;
;
;★ここから先にはエンディングが記述されています★
;ネタバレを避けたい方は閲覧しないようにご注意下さい。
;
;
;
;
```
