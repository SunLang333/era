# 口上/150 紫苑口上/KOJO_D_K150.ERB — 自动生成文档

源文件: `ERB/口上/150 紫苑口上/KOJO_D_K150.ERB`

类型: .ERB

自动摘要: functions: KOJO_TRAIN_START_D_K150, KOJO_TRAIN_END_D_K150, KOJO_COM_BEFORE_TARGET_D_K150, KOJO_COM_BEFORE_PLAYER_D_K150, KOJO_COM_D_K150, KOJO_COM_TARGET_D_K150, KOJO_COM_PLAYER_D_K150, KOJO_COM_AFTER_D_K150; UI/print

前 200 行源码片段:

```text
﻿;-------------------------------------------------
;「捕虜逆調教(外来)」の口上
;-------------------------------------------------

;=================================================
;●開始時のセリフ
;=================================================
@KOJO_TRAIN_START_D_K150
;[虚ろ]状態なら口上を表示しない
IF TALENT:虚ろ
	RETURN
ENDIF

;-------------------------------------------------
;初回 ※必ず面識アリ
;-------------------------------------------------
IF CFLAG:206 == 0
	CFLAG:206 = 1
	;PRINTFORMW 

;-------------------------------------------------
;2回目以降
;-------------------------------------------------
ELSE
	;PRINTFORMW 
ENDIF

;=================================================
;●終了時のセリフ
;=================================================
@KOJO_TRAIN_END_D_K150
;[虚ろ]状態なら口上を表示しない
IF TALENT:虚ろ
	RETURN
ENDIF

;-------------------------------------------------
;行動不能の場合
;-------------------------------------------------
;酒酔いによる行動不能
IF TCVAR:53 == 1
	;PRINTFORMW 
;快感のあまり気絶
ELSEIF TCVAR:52
	;PRINTFORMW 
;疲労による行動不能
ELSEIF TCVAR:51
	;PRINTFORMW 
;主人公が酒酔いによる行動不能
ELSEIF TCVAR:MASTER:53 == 1
	;PRINTFORMW 
;主人公が快感のあまり気絶
ELSEIF TCVAR:MASTER:52
	;PRINTFORMW 
;主人公が疲労による行動不能
ELSEIF TCVAR:MASTER:51
	;PRINTFORMW 

;-------------------------------------------------
;初回
;-------------------------------------------------
ELSEIF CFLAG:207 < 1
	;PRINTFORMW 

;-------------------------------------------------
;2回目以降
;-------------------------------------------------
ELSE
	;PRINTFORMW 
ENDIF

;-------------------------------------------------
;初回なら進行度を増加
;-------------------------------------------------
IF CFLAG:207 < 1
	CFLAG:207 = 1
ENDIF

;=================================================
;●コマンド実行前(このキャラがターゲット側のとき)
;※地の文が表示される前に実行される。戻り値に1を設定する(RETURN 1)と地の文がカットされる
;  必要に応じてKOJO_COM_TARGETの代わりに使う
;=================================================
@KOJO_COM_BEFORE_TARGET_D_K150
;[虚ろ]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:虚ろ || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

RETURN 0

;=================================================
;●コマンド実行前(このキャラがプレイヤー側のとき)
;※地の文が表示される前に実行される。戻り値に1を設定する(RETURN 1)と地の文がカットされる
;  必要に応じてKOJO_COM_PLAYERの代わりに使う
;=================================================
@KOJO_COM_BEFORE_PLAYER_D_K150
;[虚ろ]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:虚ろ || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

RETURN 0

;=================================================
;●コマンド実行時(このキャラがプレイヤーでもターゲットでも呼び出される)
;  プレイヤー・ターゲットの区別がないコマンドはここに口上を記述する
;=================================================
@KOJO_COM_D_K150
;[虚ろ]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:虚ろ || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;-------------------------------------------------
;キス
;-------------------------------------------------
IF SELECTCOM == 20
	;ファーストキス
	IF TALENT:キス未経験
		;PRINTFORMW 
	;キス経験済み
	ELSE
		;PRINTFORMW 
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;貝合わせ
;-------------------------------------------------
IF SELECTCOM == 21
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;双頭バイブ
;-------------------------------------------------
IF SELECTCOM == 22
	;このキャラと主人公が共に処女
	IF TALENT:処女 && TALENT:MASTER:処女
		;PRINTFORMW 
	;このキャラのみ処女
	ELSEIF TALENT:処女
		;PRINTFORMW 
	;主人公のみ処女
	ELSEIF TALENT:MASTER:処女
		;PRINTFORMW 
	;このキャラと主人公が共に非処女
	ELSE
		;PRINTFORMW 
	ENDIF
	RETURN 0
ENDIF

;=================================================
;●コマンド実行時(このキャラがターゲット側のとき)
;=================================================
@KOJO_COM_TARGET_D_K150
;[虚ろ]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:虚ろ || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;-------------------------------------------------
;愛撫(ターゲット)
;-------------------------------------------------
IF SELECTCOM == 0
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;胸愛撫(ターゲット)
;-------------------------------------------------
IF SELECTCOM == 1
	;Ｂ感Lv3以上
	IF ABL:Ｂ感 >= 3
		;PRINTFORMW 
	;Ｂ感Lv3未満
	ELSE
		;PRINTFORMW 
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;クンニ(ターゲット)
;-------------------------------------------------
IF SELECTCOM == 2
	;Ｃ感Lv3以上
	IF ABL:Ｃ感 >= 3
		;PRINTFORMW 
	;Ｃ感Lv3未満
	ELSE
		;PRINTFORMW 
	ENDIF
	RETURN 0
ENDIF
```
