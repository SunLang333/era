# 口上/10 レミリア口上/KOJO_FUNCTION_K10.ERB — 自动生成文档

源文件: `ERB/口上/10 レミリア口上/KOJO_FUNCTION_K10.ERB`

类型: .ERB

自动摘要: functions: KOJO_ASK_RESET_K10, CALLNAME_K10, CHECK_K10, BREAK_K10, CALL_DIRTY_K10; UI/print

前 200 行源码片段:

```text
﻿;─────────────────────────────────────── 
;●レミリア口上の使用可否・初期化
;─────────────────────────────────────── 
@KOJO_ASK_RESET_K10
#DIM SAVE_REDRAW
#DIM SAVE_COLOR
#DIM SAVE_LINE
#DIM レミリア

CURRENTREDRAW
SAVE_REDRAW = RESULT
REDRAW 0
GETCOLOR
SAVE_COLOR = RESULT
RESETCOLOR

レミリア = NAME_TO_CHARA("レミリア")

;レミリア口上用のデータを初期化
;0=未設定 1=使用しない 2=使用する
レミリア_口上デイリー入力系使用 = 0
;入力系デイリー愛称を教えて用
レミリア_主人公への呼称 '= ""
レミリア_主人公への呼称フリガナ '= ""
;入力系デイリーご褒美をあげる用
レミリア_淫語Ｐ '= "あれ"
レミリア_淫語Ｖ '= "あそこ"
レミリア_淫語Ｃ '= "くり"
レミリア_淫語Ａ '= "おしり"
レミリア_淫語Ｂ '= "さきっぽ"
;0=未設定 1=低頻度 2=高頻度
レミリア_淫語頻度 = 0
;プレイヤー側の初回実行判定（スキンヘッドチェック用）
レミリア_髪梳き初回 = 0

RESETCOLOR
PRINTL 

PRINTFORML レミリア口上使用の設定
CALL KOJO_ASK("使用しない", "使用する")
PRINTFORMW レミリア口上の使用を設定した

;使用しない
IF RESULT == 0
	CFLAG:レミリア:400 = 1
;使用する
ELSEIF RESULT == 1
	CFLAG:レミリア:400 = 2
	PRINTL 
	PRINTFORM レミリア口上デイリー入力系使用の設定
	SETCOLOR カラー_選択不可
	PRINTFORML  - あなたへの優先呼称やあなた好みの淫語を入力するイベント
	RESETCOLOR
	CALL KOJO_ASK("使用しない", "使用する")
	PRINTFORMW レミリア口上デイリー入力系使用を設定した
	;使用しない
	IF RESULT == 0
		レミリア_口上デイリー入力系使用 = 1
	;使用する
	ELSEIF RESULT == 1
		レミリア_口上デイリー入力系使用 = 2
	ENDIF
ENDIF

PRINTL 

REDRAW SAVE_REDRAW
SETCOLOR SAVE_COLOR
RETURN 0


;─────────────────────────────────────── 
;●レミリアの呼称（レミリアからレミリア_対象へ）
;　使い方：%CALLNAME_K10(レミリア_対象)%　%CALLNAME_K10(レミリア_対象, "ぁ")%
;　覚書　：第二引数に"ぁ"とつけると
;　フリガナ（CSTR:MASTER:6やレミリア_主人公への呼称フリガナや君主等への特殊呼称）がある場合
;　対応する小書き母音をつける（甘え感を出す）
;─────────────────────────────────────── 
@CALLNAME_K10(レミリア_対象, 小書き)
#FUNCTIONS
#DIM レミリア
#DIM レミリア_対象
#DIMS 小書き
#DIMS 表示テキスト
#DIMS 表示テキストフリガナ

;リセット
レミリア = NAME_TO_CHARA("レミリア")
表示テキスト '= ""

;主人公で呼称指定済み
IF レミリア_対象 == MASTER && KDVAR:レミリア:レミリア_愛称を教えて && レミリア_主人公への呼称 != ""
	表示テキスト '= レミリア_主人公への呼称
	表示テキストフリガナ '= レミリア_主人公への呼称フリガナ

;慰安中モブ相手
ELSEIF FLAG:調教モード == 調教_慰安 && CFLAG:(レミリア_対象):慰安モブ
	表示テキスト '= "みんな"
	表示テキストフリガナ '= "ミンナ"

ELSEIF CSTR:レミリア_対象:99 == "パチュリー" 
	表示テキスト '= "パチェ"
	表示テキストフリガナ '= "パチェ"

ELSEIF CSTR:レミリア_対象:99 == "霊夢" && !IS_LOVER(レミリア) && !IS_SLAVE(レミリア)
	表示テキスト '= "巫女"
	表示テキストフリガナ '= "巫女"

ELSEIF CSTR:レミリア_対象:99 == "霖之助" && !IS_LOVER(レミリア) && !IS_SLAVE(レミリア)
	表示テキスト '= "店主"
	表示テキストフリガナ '= "テンシュ"

;それ以外
ELSE
	IF CSTR:レミリア_対象:99 == "あなた"
		IF IS_MALE(レミリア_対象)
			表示テキスト '= "貴方"
			表示テキストフリガナ '= "アナタ"
		ELSE
			表示テキスト '= "貴女"
			表示テキストフリガナ '= "アナタ"
		ENDIF
	ELSE
		表示テキスト '= ANAME(レミリア_対象)
		IF CSTR:レミリア_対象:6 != ""
			表示テキストフリガナ '= CSTR:レミリア_対象:6
		ELSE
			表示テキストフリガナ '= ""
		ENDIF
	ENDIF
ENDIF

IF 小書き == "ぁ" && 表示テキストフリガナ != ""
	表示テキスト '= 表示テキスト + SMALLSCRIPT(表示テキストフリガナ)
ENDIF

RETURNF 表示テキスト


;─────────────────────────────────────── 
;●レミリア口上の分岐条件まとめ
;　IF文を短縮したり見やすくするための条件まとめ
;　よく使う条件が増えたらその都度追加
;　条件=わかりやすそうな条件名 レミリア_対象=略すと主人公
;　CHECK_K10("面識", レミリア_対象)……既知と扱う東方キャラか、面識はあるかの確認
;　CHECK_K10("陥落", レミリア_対象)……レミリア_対象が主人公か＆恋慕系服従系の陥落があるか確認する
;　CHECK_K10("紅魔館", レミリア_対象)……紅魔館のメンバーかどうか（既知かつ好意的扱い）
;　CHECK_K10("活力")……レミリアの気力体力があるまたはレミリアに主導権がある
;─────────────────────────────────────── 
@CHECK_K10(条件, レミリア_対象)
#FUNCTION
#DIMS 条件
#DIM レミリア_対象
#DIM レミリア

;レミリア_対象は未指定だと主人公
IF !レミリア_対象
	レミリア_対象 = MASTER
ENDIF

レミリア = NAME_TO_CHARA("レミリア")

;〇面識
IF 条件 == "面識"
	;レミリア_対象があなたでレミリアと面識がある（面識を得るデイリーの発生も含む）
	IF レミリア_対象 == MASTER && CFLAG:レミリア:面識
		RETURNF 1
	;東方キャラで紅魔館キャラの場合
	ELSEIF ANAME(レミリア_対象) == "レミリア" || ANAME(レミリア_対象) == "パチュリー" || ANAME(レミリア_対象) == "フラン" || ANAME(レミリア_対象) == "小悪魔" || ANAME(レミリア_対象) == "美鈴"
		RETURNF 1
	;東方キャラでレミリアが知人として扱いそうなキャラの場合（特殊な呼び方などのあるキャラ）
	ELSEIF ANAME(レミリア_対象) == "霊夢" || ANAME(レミリア_対象) == "魔理沙" || ANAME(レミリア_対象) == "霖之助"
		RETURNF 1
	;それ以外
	ELSE
		RETURNF 0
	ENDIF
ENDIF

;〇陥落
IF 条件 == "陥落"
	;レミリア_対象があなたで恋慕系か服従系の陥落
	IF レミリア_対象 == MASTER && (IS_SLAVE(レミリア) || IS_LOVER(レミリア))
		RETURNF 1
	ENDIF
ENDIF

;〇紅魔館
IF 条件 == "紅魔館"
	;レミリア_対象が東方キャラで紅魔館キャラの場合（一応レミリア自身も）
	IF ANAME(レミリア_対象) == "レミリア" || ANAME(レミリア_対象) == "パチュリー" || ANAME(レミリア_対象) == "フラン" || ANAME(レミリア_対象) == "小悪魔" || ANAME(レミリア_対象) == "美鈴" || ANAME(レミリア_対象) == "レミリア"
		RETURNF 1
	ENDIF
ENDIF

;〇活力（気力体力1/3以上または主導権持ち）
IF 条件 == "活力"
	IF BASE:レミリア:気力 > (MAXBASE:レミリア:気力 / 3) && BASE:レミリア:体力 > (MAXBASE:レミリア:体力 / 3)
		RETURNF 1
	ENDIF
```
