# 口上/143 ネムノ口上/_KOJO_EVENT_K143.ERB — 自动生成文档

源文件: `ERB/口上/143 ネムノ口上/_KOJO_EVENT_K143.ERB`

类型: .ERB

自动摘要: functions: KOJO_EVENT_K143; UI/print

前 200 行源码片段:

```text
﻿;-------------------------------------------------
;イベント口上
;-------------------------------------------------

;=================================================
;●各種イベント
;※ARGにイベント番号が入る。詳しくは資料フォルダの「era恋姫 イベント表」を参照
;※RETURNの値を0→1に変えると、デフォルトのメッセージが表示されなくなる
;=================================================
@KOJO_EVENT_K143(ARG)

;-------------------------------------------------
;ファーストキス実行　書かなくてもいい
;-------------------------------------------------
IF ARG == 1
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		;PRINTFORMW 
	;恋人
	ELSEIF TALENT:恋人
		;PRINTFORMW 
	;それ以外
	ELSE
		;PRINTFORMW 
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;告白成功
;-------------------------------------------------
IF ARG == 2
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		;PRINTFORMW 
	;それ以外
	ELSE
		;PRINTFORMW 
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;告白失敗
;-------------------------------------------------
IF ARG == 3
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し成功(酒酔いの影響・合意は得られない)
;-------------------------------------------------
IF ARG == 4
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し成功(合意を取得)
;-------------------------------------------------
IF ARG == 5
	PRINTL
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		PRINTFORML 「あっ……そんなにうちを、抱きたいのか？」
		PRINTFORMW 「…ふふっ、%ANAME(MASTER)%にならいいぞ♥」
		PRINTFORML %ANAME(TARGET)%は抵抗せず、むしろ進んで服を脱ぎ%ANAME(MASTER)%に裸体を晒す
		PRINTFORMW 「うちを、%ANAME(MASTER)%の女にしてくんろ♥」
	;それ以外
	ELSE
		PRINTFORML 「……はあ、そんなにうちを抱きたいんか？　まったくしょうがねえ奴だべ…」
		PRINTFORMW %ANAME(TARGET)%は%ANAME(MASTER)%の前で服を脱いでゆく
		PRINTFORMW 「どうせするんなら気持ちよくしてくんろ」
	ENDIF
	PRINTL
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し失敗
;-------------------------------------------------
IF ARG == 6
	PRINTFORMW 「うちはそんな安い女じゃねえだ」
	RETURN 0
ENDIF

;-------------------------------------------------
;押し倒し成功(既に合意あり)
;-------------------------------------------------
IF ARG == 7
	PRINTL 
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		SELECTCASE RAND:5
			CASE 0
				PRINTFORML 「ん…もう待ちきれねぇだか？」
				PRINTFORMW 頬を染めながら、%ANAME(TARGET)%は笑顔で抱きついてきた
			CASE 1
				PRINTFORML %ANAME(TARGET)%は服を脱ぐと、%ANAME(MASTER)%に抱きつく
				PRINTFORMW 「今日もうちに%ANAME(MASTER)%の子種をおくれ…♥」
			CASE 2
				PRINTFORML %ANAME(TARGET)%は服を脱ぎ捨てると、笑顔で%ANAME(MASTER)%に抱きついた
				PRINTFORMW 「今日もうちを気持ちよくしてくんろ」
			CASE 3
				PRINTFORML 「あっ…もう我慢できねか？　ふふっ、うちもだべ♥」
				PRINTFORMW %ANAME(TARGET)%は自ら服をはだけて、%ANAME(MASTER)%の身体に豊満な胸を押し付けてくる
			CASE 4
				PRINTFORML 「ふふ、うちが欲しいのか？　いいぞ♥」
				PRINTFORMW %ANAME(TARGET)%は%ANAME(MASTER)%を受け入れてなすがままになっている
		ENDSELECT
	;それ以外
	ELSE
		PRINTFORML 「……はあ、そんなにうちを抱きたいんか？　まったくしょうがねえ奴だべ…」
		PRINTFORMW %ANAME(TARGET)%は%ANAME(MASTER)%の前で服を脱いでゆく
		PRINTFORMW 「どうせするんなら気持ちよくしてくんろ」
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;:体力限界(通常)
;-------------------------------------------------
IF ARG == 11
	PRINTFORMW 「は、激しすぎだべ……」
	RETURN 0
ENDIF

;-------------------------------------------------
;気力限界(通常)
;-------------------------------------------------
IF ARG == 12
	PRINTFORMW 「は、激しすぎだべ……」
	RETURN 0
ENDIF

;-------------------------------------------------
;怒りの限界で追い返される
;-------------------------------------------------
IF ARG == 13
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;哀しみの限界で追い返される
;-------------------------------------------------
IF ARG == 14
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;怒りの限界で勝手に帰る
;-------------------------------------------------
IF ARG == 15
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;哀しみの限界で勝手に帰る
;-------------------------------------------------
IF ARG == 16
	;PRINTFORMW 
	RETURN 0
ENDIF

;-------------------------------------------------
;体力限界(捕虜調教)
;-------------------------------------------------
IF ARG == 17
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 「はぁ、はぁ、…す、少し休ませてくれ」
	;服従
	ELSEIF TALENT:服従
		PRINTFORML 「はぁ、はぁ……、か、堪忍してくんろ…」
	ELSE
		PRINTFORML 「はぁ、はぁ……ケダモノめっ……」
	ENDIF
	RETURN 0
ENDIF

 ;-------------------------------------------------
;気力限界(捕虜調教)
;-------------------------------------------------
IF ARG == 18
	PRINTFORMW 「あ…うぅ……」
	RETURN 0
ENDIF

 ;-------------------------------------------------
;捕虜調教で叩き起こす
;-------------------------------------------------
IF ARG == 19
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 「ふう、もう大丈夫だべ%ANAME(MASTER)%」
	;服従
```
