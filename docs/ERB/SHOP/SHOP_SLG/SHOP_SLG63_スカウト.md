# SHOP/SHOP_SLG/SHOP_SLG63_スカウト.ERB — 自动生成文档

源文件: `ERB/SHOP/SHOP_SLG/SHOP_SLG63_スカウト.ERB`

类型: .ERB

自动摘要: functions: SHOP_SLG_NAME63, SHOP_SLG_CHECK63, SHOP_SLG_EVENTBUY63, SLG_SCOUT, SCOUT_SHOW_INFO, SCOUT_CALC_PRICE, SCOUT_CALC_PRICE_BASIS, SCOUT_CALC_PRICE_TECHNOLOGY_CORRECTION, SCOUT_PREPARE_CANDIDATES, SELECT_CHARA_RANDOM_LOGIC_SCOUT_PREPARE_CANDIDATES, SCOUT_JUDGE_PAYMENT_DISCOUNT, SCOUT_JUDGE_PAYMENT_PLUS; assigns RESULTS; UI/print

前 200 行源码片段:

```text
﻿;-------------------------------------------------
;「スカウト」の名称
;-------------------------------------------------
@SHOP_SLG_NAME63
RESULTS:0 '= "スカウト"

;-------------------------------------------------
;「スカウト」の選択可否
;-------------------------------------------------
@SHOP_SLG_CHECK63
SIF CFLAG:MASTER:所属 == 0
	RETURN 0
;プレイヤーが特殊勢力所属なら駄目
SIF IS_SP_COUNTRY(CFLAG:MASTER:所属)
	RETURN 0
;候補がいればok
FOR LOCAL, 0, VARSIZE("SCOUT_CANDIDATES")
	SIF ID_TO_CHARA(SCOUT_CANDIDATES:LOCAL) != -1
		RETURN 1
NEXT
RETURN 0

;-------------------------------------------------
;「スカウト」の左カラムメニューの入力処理
;-------------------------------------------------
@SHOP_SLG_EVENTBUY63
CALL SLG_SCOUT
RETURN 1

;-------------------------------------------------
;「スカウト」本体
;-------------------------------------------------
@SLG_SCOUT
#DIM 対象
#DIM 対象添え字
#DIM FIRST_LINE
FIRST_LINE = LINECOUNT
VARSET SHOP_CHARA_LIST, -1
FOR LOCAL, 0, VARSIZE("SCOUT_CANDIDATES")
	SHOP_CHARA_LIST:LOCAL = ID_TO_CHARA(SCOUT_CANDIDATES:LOCAL)
NEXT

CALL SINGLE_DRAWLINE
PRINTFORML  自国の強化のため、在野の人材をスカウトしてこよう……
PRINTFORML  いま候補に挙がっている人材は以下の通りだが、誰をスカウトしようか……
SIF !SCOUT_COUNT
	CALL COLOR_PRINTL("初回スカウトのため、★6以下のキャラの提示額が大幅割引中（軍事行動解禁まで）", カラー_注意)
SETCOLOR カラー_選択不可
PRINTFORM  所持金:
SETCOLOR カラー_選択中
IF STRLENS(NUM_FORMAT(MONEY)) <= 11
	PRINTFORML %NUM_FORMAT(MONEY), 11%
ELSE
	PRINTFORML %NUM_FORMAT(MONEY)%
ENDIF
RESETCOLOR
CALL SINGLE_DRAWLINE

FOR LOCAL, 0, VARSIZE("SCOUT_CANDIDATES")
	LOCAL:1 = ID_TO_CHARA(SCOUT_CANDIDATES:LOCAL)
	IF LOCAL:1 == -1
		CONTINUE
	ENDIF
	;何らかの理由で（データ操作とか）放浪していない場合、パージ
	IF CFLAG:(LOCAL:1):特殊状態 != 特殊状態_放浪
		SCOUT_CANDIDATES:(LOCAL) = 0
		CONTINUE
	ENDIF
	CALL SCOUT_SHOW_INFO(LOCAL:1, LOCAL)
	SETCOLOR カラー_灰色
	CALL SINGLE_DRAWLINE
	RESETCOLOR
NEXT
PRINTPLAIN  
PRINTBUTTON @"[%"戻る", MAX_CHARANAME_LENGTH, LEFT%]", 99

$INPUT_LOOP
INPUT
IF RESULT == 99
	RETURN 0
ENDIF

IF RESULT < 0
	CLEARLINE 1
	GOTO INPUT_LOOP
ELSEIF 100 <= RESULT && RESULT < VARSIZE("SCOUT_CANDIDATES") + 100
	対象添え字 = RESULT - 100
	対象 = ID_TO_CHARA(SCOUT_CANDIDATES:対象添え字)
	IF 対象 == -1
		CLEARLINE 1
		GOTO INPUT_LOOP
	ENDIF
	CALL SINGLE_DRAWLINE
	CALL SHOW_INFO_WITH_UI(対象, 1)
	CLEARLINE LINECOUNT - FIRST_LINE
	RESTART
ELSEIF VARSIZE("SCOUT_CANDIDATES") <= RESULT
	CLEARLINE 1
	GOTO INPUT_LOOP
ENDIF

対象 = ID_TO_CHARA(SCOUT_CANDIDATES:RESULT)
対象添え字 = RESULT


IF 対象 == -1
	CLEARLINE 1
	GOTO INPUT_LOOP
ENDIF

PRINTFORML %ANAME(対象)%に仕官しないかともちかけた……
PRINTFORML %ANAME(対象)%は金%NUM_FORMAT(SCOUT_PRICE:対象添え字)%を要求してきた
PRINTL
CALL ASK_MULTI_JUDGE("要求額を支払う", SCOUT_PRICE:対象添え字 <= MONEY, "支払い金額を入力する", 1, "やめる", 1)
IF RESULT == 2
	CLEARLINE LINECOUNT - FIRST_LINE
	RESTART
ENDIF

IF RESULT == 0
	LOCAL = SCOUT_PRICE:対象添え字
ELSE
	PRINTFORM いくら支払う……？ (所持金:{MONEY}) 要求額:
	PRINTBUTTON @"[{SCOUT_PRICE:対象添え字}]", SCOUT_PRICE:対象添え字
	$PAYMENT_LOOP
	INPUT
	LOCAL = RESULT
	SIF LOCAL > MONEY
		GOTO PAYMENT_LOOP
ENDIF

IF !SCOUT_JUDGE_PAYMENT_DISCOUNT(対象, SCOUT_PRICE:対象添え字, LOCAL)
	PRINTFORMW %ANAME(対象)%に、そんな額では仕官できないと返された……
	SCOUT_CANDIDATES:対象添え字 = 0
	RESTART
ENDIF

PRINTFORML 戦力補強のためなら安いものだ……
PRINTFORML %ANAME(対象)%に金{LOCAL}を支払った

LOCAL:1 = SCOUT_JUDGE_PAYMENT_PLUS(対象, SCOUT_PRICE:対象添え字, LOCAL)

IF LOCAL:1 && SCOUT_COUNT
	PRINTFORML %ANAME(対象)%はこんなにもらえるなんて！　と喜んでいるようだ……
	PRINTFORML %ANAME(対象)%の能力が上昇した
	FOR LOCAL:2, 0, LOCAL:1
		SELECTCASE RAND:6
			CASE 0
				ABL:対象:武闘 ++
			CASE 1
				ABL:対象:防衛 ++
			CASE 1
				ABL:対象:知略 ++
			CASE 2
				ABL:対象:政治 ++
			CASE 3
				ABL:対象:歌唱 ++
			CASE 4
				ABL:対象:料理 ++
		ENDSELECT
	NEXT
ENDIF

PRINTFORML %ANAME(対象)%と固い握手を交わした……
CALL COLOR_PRINTW(@"%ANAME(対象)%が仲間になりました", カラー_注意)

MONEY -= LOCAL
CALL CHANGE_COUNTRY(対象, CFLAG:MASTER:所属, 1)
CFLAG:対象:好感度 = MAX(300, CFLAG:対象:好感度 + 300)
CFLAG:対象:従属度 = MAX(100, CFLAG:対象:従属度 + 100)
CFLAG:対象:依存度 = MAX(50, CFLAG:対象:依存度 + 50)

SCOUT_CANDIDATES:(対象添え字) = 0

;回数増やして再計算
SCOUT_COUNT ++
FOR LOCAL, 0, VARSIZE("SCOUT_CANDIDATES")
	対象 = ID_TO_CHARA(SCOUT_CANDIDATES:LOCAL)
	SIF 対象 != -1
		SCOUT_PRICE:LOCAL = SCOUT_CALC_PRICE(対象)
NEXT
RETURN

;-------------------------------------------------
;スカウト対象の情報表示＋ボタン生成
;-------------------------------------------------
@SCOUT_SHOW_INFO(対象, 番号)
#DIM 対象
#DIM 番号

PRINTPLAIN  
PRINTBUTTON @"[%ANAME(対象), MAX_CHARANAME_LENGTH, LEFT%]", 番号
PRINTPLAIN   
PRINTBUTTON "[能力表示]", 番号 + 100
SIF MONEY < SCOUT_PRICE:番号
	CALL COLOR_PRINT("資金不足", カラー_警告)
PRINTL 
;提示額の縦列を目立たせ右揃えに
SETCOLOR カラー_選択不可
PRINTFORM  提示額:
```
