# SHOP/SHOP_LIFE/SHOP_LIFE71_アイテム.ERB — 自动生成文档

源文件: `ERB/SHOP/SHOP_LIFE/SHOP_LIFE71_アイテム.ERB`

类型: .ERB

自动摘要: functions: SHOP_LIFE_NAME71, SHOP_LIFE_CHECK71, SHOP_LIFE_EVENTBUY71, SHOP_LIFE_EVENTBUY_COLUMN_RIGHT_OFF_CHECK71, SHOP_ITEM, ITEM_EXPLANATION, PROMPTLY_BUTTONS, ADD_ANIMAL; assigns RESULTS; UI/print

前 200 行源码片段:

```text
﻿;-------------------------------------------------
;「アイテム」の名称
;-------------------------------------------------
@SHOP_LIFE_NAME71
RESULTS:0 '= "アイテム"

;-------------------------------------------------
;「アイテム」の選択可否
;-------------------------------------------------
@SHOP_LIFE_CHECK71
SIF CFLAG:MASTER:捕虜先
	RETURN 0
RETURN 1

;-------------------------------------------------
;「アイテム」の左カラムメニューの入力処理
;-------------------------------------------------
@SHOP_LIFE_EVENTBUY71
CALL SHOP_ITEM
RETURN 1

;-------------------------------------------------
;「アイテム」の右カラムオフ判定
;-------------------------------------------------
@SHOP_LIFE_EVENTBUY_COLUMN_RIGHT_OFF_CHECK71

;-------------------------------------------------
;「アイテム」の処理
;-------------------------------------------------
@SHOP_ITEM
#DIM FIRST_LINE_ITEM_MAIN
#DIM IS_GISHI
#DIM ITEM_NUM_DISPFLAG

REDRAW 0
FIRST_LINE_ITEM_MAIN = LINECOUNT

;[技師]持ちが主人公勢力に存在するかどうかを判定
SIF TALENT:MASTER:技師
	IS_GISHI = 1

;タイトル
CALL SINGLE_DRAWLINE
PRINTPLAIN  
CALL COLUMN_RIGHT_TITLE("購入/開発/売却するアイテムを選択", "", "", "0", "200～全")
PRINTL 
CALL SINGLE_DRAWLINE

;所持アイテム表示
CALL COLOR_PRINT(" 所持 ", カラー_注釈, "金:", カラー_選択不可, @"%NUM_FORMAT(MONEY)%", カラー_選択中)

LOCAL:1 = 999
FOR LOCAL:0, 100, 300
	IF ITEM:(LOCAL:0) >= 1
		IF LOCAL:1 > 70
			LOCAL:1 = 0
			PRINTL 
			PRINTFORM %TOSTR_SPACE(6)%
		ENDIF
		LOCALS:0 = %ITEMNAME:(LOCAL:0)%
		IF ITEM:(LOCAL:0) >= 2
			LOCALS:0 = %LOCALS:0%×{ITEM:(LOCAL:0)}
		ENDIF
		LOCAL:1 += STRLENS(LOCALS:0) + 2
		PRINTFORM %LOCALS:0%　
	ENDIF
NEXT
IF LOCAL:1 == 999
	PRINTL 
	PRINTFORM %TOSTR_SPACE(6)%
	CALL COLOR_PRINT("所持アイテムなし", カラー_選択不可)
ENDIF
PRINTL 

SETCOLOR カラー_灰色
CALL SINGLE_DRAWLINE
RESETCOLOR

CALL COLOR_PRINT(" 購入 ", カラー_注釈, "選択後に説明表示", カラー_選択不可)
PRINTL 
PRINTFORM %TOSTR_SPACE(6)%
LOCAL:1 = 0
FOR LOCAL:0, 100, 300
	IF ITEMNAME:(LOCAL:0) != "" && ITEMSALES:(LOCAL:0)
		IF LOCAL:1 > 0 && LOCAL:1 % 3 == 0
			PRINTL 
			PRINTFORM %TOSTR_SPACE(6)%
		ELSEIF LOCAL:1 > 0
			PRINTFORM %TOSTR_SPACE(9)%
		ENDIF
		LOCAL:1 ++
		CALL GET_PRICE(LOCAL:0)
		;所持金不足 or 所持数オーバーで購入できない時は色を変える（がクリックはできる）
		IF MONEY < RESULT || ITEM:(LOCAL:0) >= 99
			SETCOLOR カラー_選択不可
			PRINTBUTTON @"[%ITEMNAME:(LOCAL:0), MAX_CHARANAME_LENGTH, LEFT%]%NUM_FORMAT(RESULT), 8%", LOCAL:0
			RESETCOLOR
		ELSE
			PRINTBUTTON @"[%ITEMNAME:(LOCAL:0), MAX_CHARANAME_LENGTH, LEFT%]%NUM_FORMAT(RESULT), 8%", LOCAL:0
		ENDIF
	ENDIF
NEXT
PRINTL 

SETCOLOR カラー_灰色
CALL SINGLE_DRAWLINE
RESETCOLOR
CALL COLOR_PRINT(@" 開発 ", カラー_注釈)

IF IS_GISHI
	CALL COLOR_PRINT(@"素質", カラー_選択不可, "<技師>割引", カラー_緑, "あり", カラー_選択不可, @"%"開発費", 20%%TOSTR_SPACE(2)%%"期間", 6%%TOSTR_SPACE(7)%%"開発費", MAX_CHARANAME_LENGTH + 21%%TOSTR_SPACE(2)%%"期間", 6%", カラー_注釈)
ELSE
	CALL COLOR_PRINT("素質<技師>割引なし", カラー_選択不可, @"%"開発費", 10%%TOSTR_SPACE(2)%%"期間", 4%%TOSTR_SPACE(9)%%"開発費", MAX_CHARANAME_LENGTH + 11%%TOSTR_SPACE(2)%%"期間", 4%", カラー_注釈)
ENDIF
PRINTL 

LOCAL:1 = 0
FOR LOCAL:0, 200, 300
	IF ITEMNAME:(LOCAL:0) != "" && !ITEMSALES:(LOCAL:0) && ITEMTIME:(LOCAL:0) == 0 && (!INRANGE(LOCAL:0, 200, 249) || ITEM:LOCAL == 0)
		RESULT = 1
		TRYCALLFORM ITEM_ABLE{LOCAL:0}
		IF RESULT
			IF LOCAL:1 == 0
				PRINTFORM %TOSTR_SPACE(6)%
			;２列で改行
			ELSEIF LOCAL:1 > 0 && LOCAL:1 % 2 == 0
				PRINTL 
				PRINTFORM %TOSTR_SPACE(6)%
			;技師ありだと横幅を広くとるので合間のスペースは狭めにとる
			ELSEIF LOCAL:1 > 0 && IS_GISHI
				PRINTFORM %TOSTR_SPACE(7)%
			;技師なしだと横幅が狭いので合間のスペースは広めにとる
			ELSEIF LOCAL:1 > 0
				PRINTFORM %TOSTR_SPACE(9)%
			ENDIF
			LOCAL:1 ++
			IF IS_GISHI
				IF MONEY < ITEMPRICE:(LOCAL:0) * 75 / 100
					SETCOLOR カラー_選択不可
					PRINTBUTTON @"[%ITEMNAME:(LOCAL:0), MAX_CHARANAME_LENGTH, LEFT%]", LOCAL:0
					PRINTPLAIN  
					PRINTPLAINFORM %NUM_FORMAT(ITEMPRICE:(LOCAL:0)), 8%
					PRINTPLAINFORM →%NUM_FORMAT((ITEMPRICE:(LOCAL:0) * 75 / 100)), 8%
					PRINTPLAINFORM %TOSTR_SPACE(2)%
					PRINTPLAINFORM {ITEMTERM:(LOCAL:0), 2}
					PRINTPLAINFORM →{MAX(ITEMTERM:(LOCAL:0) / 2, 1), 2}
					RESETCOLOR
				ELSE
					PRINTBUTTON @"[%ITEMNAME:(LOCAL:0), MAX_CHARANAME_LENGTH, LEFT%]", LOCAL:0
					PRINTPLAIN  
					PRINTFORM %NUM_FORMAT(ITEMPRICE:(LOCAL:0)), 8%
					SETCOLOR カラー_緑
					PRINTFORM →%NUM_FORMAT((ITEMPRICE:(LOCAL:0) * 75 / 100)), 8%
					RESETCOLOR
					PRINTPLAINFORM %TOSTR_SPACE(2)%
					PRINTFORM {ITEMTERM:(LOCAL:0), 2}
					SETCOLOR カラー_緑
					PRINTFORM →{MAX(ITEMTERM:(LOCAL:0) / 2, 1), 2}
					RESETCOLOR
				ENDIF
			ELSE
				IF MONEY < ITEMPRICE:(LOCAL:0)
					SETCOLOR カラー_選択不可
					PRINTBUTTON @"[%ITEMNAME:(LOCAL:0), MAX_CHARANAME_LENGTH, LEFT%]", LOCAL:0
					PRINTPLAIN  
					PRINTFORM %NUM_FORMAT(ITEMPRICE:(LOCAL:0)), 8%
					PRINTPLAINFORM %TOSTR_SPACE(2)%
					PRINTFORM {ITEMTERM:(LOCAL:0), 4}
					RESETCOLOR
				ELSE
					PRINTBUTTON @"[%ITEMNAME:(LOCAL:0), MAX_CHARANAME_LENGTH, LEFT%]", LOCAL:0
					PRINTPLAIN  
					PRINTFORM %NUM_FORMAT(ITEMPRICE:(LOCAL:0)), 8%
					PRINTPLAINFORM %TOSTR_SPACE(2)%
					PRINTFORM {ITEMTERM:(LOCAL:0), 4}
				ENDIF
			ENDIF
		ENDIF
	ENDIF
NEXT
IF LOCAL:1 != 0
	PRINTL 
ENDIF

CALL SINGLE_DRAWLINE("-", カラー_灰色)
CALL COLOR_PRINTL(" 開発中のアイテム ", カラー_注釈)
CALL SINGLE_DRAWLINE("-", カラー_灰色)

LOCAL:1 = 0
FOR LOCAL, 0, VARSIZE("ITEMTIME")
	SIF ITEMTIME:LOCAL == 0
		CONTINUE
	IF LOCAL:1 == 0
		PRINTFORM %TOSTR_SPACE(6)%
	;3列で改行
	ELSEIF LOCAL:1 > 0 && LOCAL:1 % 4 == 0
		PRINTL 
		PRINTFORM %TOSTR_SPACE(6)%
	ENDIF
	PRINTFORM %ITEMNAME:(LOCAL:0), MAX_CHARANAME_LENGTH, LEFT%
```
